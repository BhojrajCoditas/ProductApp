<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">

    <fragment
        android:id="@+id/homeFragment"
        android:name="com.example.productapp.ui.home.HomeFragment"
        android:label="Home"
        app:defaultNavHost="true">
        <action
            android:id="@+id/action_homeFragment_to_productDetailFragment"
            app:destination="@id/productDetailFragment"
            app:enterAnim="@anim/slide_in_right"
            app:exitAnim="@anim/slide_out_left"
            app:popEnterAnim="@anim/slide_in_left"
            app:popExitAnim="@anim/slide_out_right" />
    </fragment>

    <fragment
        android:id="@+id/productDetailFragment"
        android:name="com.example.productapp.ui.detail.ProductDetailFragment"
        android:label="Product Detail">
        <argument
            android:name="product"
            app:argType="com.example.productapp.data.model.Product" /> <!-- Pass Product Object -->
    </fragment>

    <fragment
        android:id="@+id/profileFragment"
        android:name="com.example.productapp.ui.profile.ProfileFragment"
        android:label="Profile" />

</navigation>
